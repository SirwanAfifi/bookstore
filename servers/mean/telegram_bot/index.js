require('./bot');
require('./web');

/*var movieService = require('./services/bookService.js');

movieService.getBookByTitle('Hooked', (err, result) => {
    result.forEach(item =>{
        console.log(item.best_book);
    }, this);
});*/